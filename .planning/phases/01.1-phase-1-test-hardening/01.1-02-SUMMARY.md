---
phase: 01.1-phase-1-test-hardening
plan: 02
subsystem: testing
tags: [pytest, integration-test, pipeline, tdd, app-factory, vad, chunker, transcript]

# Dependency graph
requires:
  - phase: 01-streaming-capture
    provides: SessionManager, VadDetector, AudioChunker, TranscriptWriter, WhisperService, speaker classifier
  - phase: 01.1-phase-1-test-hardening plan 01
    provides: Shared fakes (FakeAudioCapture, FakeWhisperService, FakeVadModel) in conftest.py
provides:
  - App factory structural tests (7 tests) verifying create_app() contract
  - Full pipeline integration test (5 tests) proving end-to-end audio-to-transcript data flow
  - Regression safety net catching "units pass but pipeline doesn't connect" bugs
affects: [01.1-phase-1-test-hardening, 02-clinical-extraction]

# Tech tracking
tech-stack:
  added: []
  patterns: [pipeline integration testing with real components and fake hardware boundaries, app factory structural testing]

key-files:
  created:
    - tests/test_main.py
    - tests/test_pipeline_integration.py
  modified: []

key-decisions:
  - "Integration test uses real VadDetector with FakeVadModel injected via _model attribute replacement, not mock.patch"
  - "Tuned Settings (short chunk duration, small silence gap) for fast integration test execution without hundreds of audio blocks"
  - "Lifespan shutdown tested structurally via FakeSessionManager state transitions rather than asgi-lifespan library"

patterns-established:
  - "Pipeline integration test pattern: real orchestrator + real processing components + fake hardware boundaries (audio capture, GPU model, VAD model)"
  - "App factory structural testing: inspect route paths, mount points, and configuration without running lifespan"

requirements-completed: [AUD-01, TRX-01, TRX-02, PRV-01]

# Metrics
duration: 4min
completed: 2026-03-08
---

# Phase 1.1 Plan 02: App Factory Tests and Pipeline Integration Test Summary

**7 app factory structural tests plus 5 pipeline integration tests proving end-to-end data flow from audio blocks through VAD, chunker, whisper, stitcher, speaker classifier, and transcript writer to disk**

## Performance

- **Duration:** 4 min
- **Started:** 2026-03-08T03:20:30Z
- **Completed:** 2026-03-08T03:25:12Z
- **Tasks:** 2
- **Files modified:** 2

## Accomplishments
- Added 7 structural tests for create_app() covering title, router inclusion, static mount, lifespan, module-level app instance, and shutdown behavior
- Added 5 pipeline integration tests that prove the full Phase 1 chain connects: audio blocks -> VadDetector -> AudioChunker -> WhisperService -> deduplicate_overlap -> classify_speaker -> TranscriptWriter -> file on disk
- Integration test uses real VadDetector (with FakeVadModel), real AudioChunker, real TranscriptWriter, and real SessionManager -- only hardware boundaries are faked
- Full test suite grew from 104 to 116 tests with zero regressions
- Every production module now has a dedicated test file

## Task Commits

Each task was committed atomically:

1. **Task 1: Write create_app and lifespan tests** - `564b17b` (test)
2. **Task 2: Write pipeline integration test** - `5a6a0ed` (test)

## Files Created/Modified
- `tests/test_main.py` - 7 structural tests for app factory: title, router, static mount, lifespan, shutdown, module-level instance
- `tests/test_pipeline_integration.py` - 5 integration tests proving full pipeline data flow with real components and fake hardware boundaries

## Decisions Made
- Used real VadDetector with FakeVadModel injected via direct `_model` attribute replacement rather than `unittest.mock.patch`. This is simpler and keeps the test focused on proving component connectivity.
- Tuned Settings for integration test: `max_chunk_duration_secs=1`, `silence_gap_secs=0.2`, `overlap_secs=0.1` -- ensures chunks finalize quickly with only 12 audio blocks instead of needing 200+ blocks at default settings.
- Tested lifespan shutdown structurally with FakeSessionManager rather than using asgi-lifespan library (not in dependencies). The test verifies the shutdown contract: if `is_active()` then `stop()` is called.

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
None.

## User Setup Required
None - no external service configuration required.

## Next Phase Readiness
- All production modules now have dedicated test files
- Pipeline integration test serves as regression safety net for Phase 2 work
- Ready for Plan 01.1-03 (human verification checkpoint)
- Test suite is 116 tests and provides a reliable signal: if tests pass, the pipeline works

## Self-Check: PASSED

All 4 files verified present. Both task commits (564b17b, 5a6a0ed) confirmed in git log. 116 tests passing.

---
*Phase: 01.1-phase-1-test-hardening*
*Completed: 2026-03-08*
