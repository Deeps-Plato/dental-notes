---
phase: 01.1-phase-1-test-hardening
plan: 03
type: execute
wave: 3
depends_on:
  - 01.1-01
  - 01.1-02
files_modified: []
autonomous: false
requirements:
  - AUD-01
  - TRX-01
  - TRX-02
  - PRV-01

must_haves:
  truths:
    - "User confirms the app starts on real Windows hardware and displays the web UI"
    - "User confirms real microphone audio is captured and transcribed with dental vocabulary"
    - "User confirms transcript appears in the browser with Doctor/Patient speaker labels"
    - "User confirms transcript persists after stopping the session"
    - "User confirms no network requests are made during the session"
  artifacts: []
  key_links: []
---

<objective>
Complete the human verification checkpoint that proves Phase 1 works on real hardware with a real microphone.

Purpose: "Tests pass" is necessary but not sufficient. The human must confirm the app actually works -- captures real audio, transcribes with Whisper on the GPU, shows transcript in the browser with speaker labels, and saves the transcript file. This was the uncompleted Plan 01-03 Task 3 checkpoint, now folded into Phase 1.1 as its final gate.

Output: Human confirmation that Phase 1 delivers working software on real hardware.
</objective>

<execution_context>
@/home/amandeep_chadda/.claude/get-shit-done/workflows/execute-plan.md
@/home/amandeep_chadda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01.1-phase-1-test-hardening/01.1-CONTEXT.md
@.planning/phases/01.1-phase-1-test-hardening/01.1-01-SUMMARY.md
@.planning/phases/01.1-phase-1-test-hardening/01.1-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full test suite and confirm green</name>
  <files></files>
  <action>
    Run the complete test suite to confirm all unit tests and the pipeline integration test pass before asking the user for manual verification.

    ```bash
    cd ~/claude/dental-notes && .venv/bin/python -m pytest tests/ --tb=short -v
    ```

    If any test fails, diagnose and report the failure before proceeding to the checkpoint. All tests MUST be green before the human verification gate.

    Also run ruff lint check:
    ```bash
    cd ~/claude/dental-notes && .venv/bin/python -m ruff check src/ tests/
    ```
  </action>
  <verify>
    <automated>cd ~/claude/dental-notes && .venv/bin/python -m pytest tests/ --tb=short -q && .venv/bin/python -m ruff check src/ tests/</automated>
  </verify>
  <done>All tests pass, no lint errors</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification -- Phase 1 works on real hardware</name>
  <files></files>
  <action>
    Present the human verification checkpoint to the user. This is a blocking gate -- Phase 1 is not done until the human confirms it works.

    What was built: Complete Phase 1 pipeline (audio capture -> VAD -> chunker -> Whisper transcription -> speaker classification -> transcript file). Test suite hardened with unit tests for all production modules and a pipeline integration test proving components connect end-to-end.

    Steps for the user to verify:

    Prerequisites: Windows machine with NVIDIA GPU and Yeti Classic microphone.

    1. Start the server on Windows Python:
       cd C:\Users\Amandeep Chadda\claude\dental-notes
       python start_server.py

    2. Open browser to http://localhost:8000

    3. Verify the web UI loads: page title "Dental Notes", microphone selector dropdown, Start Recording button, audio level bar.

    4. Click "Start Recording" and speak dental phrases like "The patient presents with sensitivity on tooth number 14" and "I recommend a porcelain crown, CDT code D2740". Wait a few seconds for transcription.

    5. Verify transcript appears with speaker labels (Doctor:/Patient:), each turn as a separate paragraph, dental terminology correct.

    6. Click "Stop Recording" -- transcript remains visible, file path shown, check transcripts/ directory.

    7. Verify privacy: no network requests in browser DevTools (F12 -> Network tab).

    8. Test session lifecycle: Start -> Pause -> Resume -> Stop all work via UI buttons.
  </action>
  <verify>User confirms all 8 verification steps pass by typing "approved"</verify>
  <done>Human confirms the app works on real Windows hardware with real microphone -- Phase 1 verified</done>
</task>

</tasks>

<verification>
Phase 1.1 is complete when:
1. All automated tests pass (unit + integration)
2. Human confirms the app works on real Windows hardware with real microphone
3. Transcript files are produced with speaker labels and dental terminology
</verification>

<success_criteria>
- Full test suite green (all existing + new tests from Plans 01 and 02)
- Human confirms: server starts, UI loads, audio captures, transcript appears with speaker labels, dental terms recognized, transcript persists after stop, no network requests
- Phase 1 is proven to deliver working software
</success_criteria>

<output>
After completion, create `.planning/phases/01.1-phase-1-test-hardening/01.1-03-SUMMARY.md`
</output>
