---
phase: 03-review-and-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dental_notes/session/store.py
  - src/dental_notes/clinical/models.py
  - src/dental_notes/clinical/prompts.py
  - src/dental_notes/clinical/formatter.py
  - src/dental_notes/config.py
  - tests/test_session_store.py
  - tests/test_note_formatter.py
  - tests/conftest.py
autonomous: true
requirements: [REV-01, REV-02, REV-03, AUD-02]

must_haves:
  truths:
    - "Sessions can be created, loaded, listed, updated, and deleted as JSON files"
    - "Transcript file is deleted when a session is finalized"
    - "SOAP note can be formatted as plain text with section headers for clipboard"
    - "SoapNote model includes medications and va_narrative fields"
    - "Session list returns sessions sorted by creation time descending"
  artifacts:
    - path: "src/dental_notes/session/store.py"
      provides: "SessionStore + SavedSession + SessionStatus"
      exports: ["SessionStore", "SavedSession", "SessionStatus"]
    - path: "src/dental_notes/clinical/formatter.py"
      provides: "NoteFormatter for clipboard text"
      exports: ["format_note_for_clipboard", "format_section"]
    - path: "src/dental_notes/clinical/models.py"
      provides: "Enriched SoapNote with medications + va_narrative"
      contains: "medications"
    - path: "tests/test_session_store.py"
      provides: "Session persistence tests"
      min_lines: 80
    - path: "tests/test_note_formatter.py"
      provides: "Clipboard formatting tests"
      min_lines: 40
  key_links:
    - from: "src/dental_notes/session/store.py"
      to: "src/dental_notes/clinical/models.py"
      via: "SavedSession.extraction_result uses ExtractionResult"
      pattern: "ExtractionResult"
    - from: "src/dental_notes/clinical/formatter.py"
      to: "src/dental_notes/clinical/models.py"
      via: "format_note_for_clipboard reads SoapNote fields"
      pattern: "SoapNote|ExtractionResult"
    - from: "src/dental_notes/session/store.py"
      to: "pathlib.Path"
      via: "finalize_session deletes transcript file"
      pattern: "unlink.*missing_ok"
---

<objective>
Build the data layer for Phase 3: session persistence (JSON files), enriched clinical models (medications, VA narrative), and clipboard text formatting.

Purpose: Downstream plans need SavedSession, SessionStore, and NoteFormatter to build the review UI and export flow. This plan creates all backend contracts before any routes or templates.
Output: SessionStore with CRUD + finalize, enriched SoapNote model, NoteFormatter, and full test coverage for all three.
</objective>

<execution_context>
@/home/amandeep_chadda/.claude/get-shit-done/workflows/execute-plan.md
@/home/amandeep_chadda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-review-and-export/03-CONTEXT.md
@.planning/phases/03-review-and-export/03-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/dental_notes/clinical/models.py:
```python
class CdtCode(BaseModel):
    code: str = Field(pattern=r"^D\d{4}$", description="CDT code, e.g. D2391")
    description: str = Field(description="Procedure description")

class SpeakerChunk(BaseModel):
    chunk_id: int
    speaker: str = Field(description="Doctor or Patient")
    text: str

class SoapNote(BaseModel):
    subjective: str
    objective: str
    assessment: str
    plan: str
    cdt_codes: list[CdtCode]
    clinical_discussion: list[str]

class ExtractionResult(BaseModel):
    soap_note: SoapNote
    speaker_chunks: list[SpeakerChunk]
    clinical_summary: str
```

From src/dental_notes/config.py:
```python
class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="DENTAL_")
    storage_dir: Path = Path("transcripts")
    # ... other fields
```

From src/dental_notes/session/manager.py:
```python
class SessionManager:
    def get_chunks(self, start: int = 0) -> list[tuple[str, str]]: ...
    def stop(self) -> Path: ...
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Session persistence layer + enriched SoapNote model</name>
  <files>src/dental_notes/session/store.py, src/dental_notes/clinical/models.py, src/dental_notes/clinical/prompts.py, src/dental_notes/config.py, tests/test_session_store.py, tests/conftest.py</files>
  <behavior>
    - Test: create_session() saves JSON file to sessions_dir, returns SavedSession with status RECORDED
    - Test: get_session(id) loads session from JSON file, returns SavedSession or None
    - Test: list_sessions() returns all sessions sorted by created_at descending
    - Test: update_session() writes updated session back to disk with new updated_at
    - Test: delete_session() removes JSON file from disk
    - Test: finalize_session() deletes transcript .txt file via Path.unlink(missing_ok=True) and updates status
    - Test: finalize with already-deleted transcript file succeeds (missing_ok=True)
    - Test: atomic write uses temp file then os.replace
    - Test: SoapNote now includes medications: list[str] and va_narrative: str | None fields
    - Test: ExtractionResult with enriched SoapNote validates correctly
  </behavior>
  <action>
    1. Update SoapNote in src/dental_notes/clinical/models.py:
       - Add `medications: list[str] = Field(default_factory=list, description="Prescribed medications extracted from transcript")`
       - Add `va_narrative: str | None = Field(default=None, description="Per-tooth narrative for VA patients (auto-detected from transcript context)")`
       - These are optional/defaulted fields so existing tests and extraction pipeline remain backward-compatible

    2. Update EXTRACTION_SYSTEM_PROMPT in src/dental_notes/clinical/prompts.py:
       - After the Clinical Discussion section, add a Prescribed Medications section: "Extract any medications prescribed or discussed. List drug name, dosage, frequency, and duration if mentioned. Return empty list if no medications discussed."
       - Add VA Patient Detection section: "If the transcript mentions VA (Veterans Affairs), generate a per-tooth narrative section summarizing findings and indicated procedures for each tooth discussed. Return null if VA is not mentioned."

    3. Add `sessions_dir: Path = Path("sessions")` to Settings in config.py

    4. Create src/dental_notes/session/store.py with:
       - SessionStatus enum: RECORDED, EXTRACTED, REVIEWED
       - SavedSession Pydantic model: session_id (str, uuid4), created_at (datetime), updated_at (datetime), status (SessionStatus), transcript_path (str), chunks (list[tuple[str, str]]), extraction_result (ExtractionResult | None), edited_note (dict | None), transcript_dirty (bool = False)
       - SessionStore class with sessions_dir: Path in __init__ (mkdir parents=True, exist_ok=True)
       - create_session(chunks, transcript_path) -> SavedSession
       - get_session(session_id) -> SavedSession | None
       - list_sessions() -> list[SavedSession] (sorted by created_at descending)
       - update_session(session) -> None (updates updated_at, atomic write)
       - delete_session(session_id) -> None (Path.unlink(missing_ok=True))
       - finalize_session(session_id) -> None: deletes transcript file (Path.unlink(missing_ok=True)), deletes session JSON file
       - _write(session) -> None: atomic write via temp file + os.replace

    5. Create tests/test_session_store.py with tests for all behaviors listed above

    6. Update tests/conftest.py:
       - Add FakeSessionStore class mirroring SessionStore interface (in-memory dict)
       - Add sample_saved_session fixture returning a SavedSession with sample extraction result
       - Update FakeOllamaService response_data to include medications and va_narrative fields in soap_note
       - Add sessions_dir to test_settings fixture (tmp_path / "sessions")

    Per user decision: medications section always at bottom of note. VA narrative is conditional (auto-detected).
  </action>
  <verify>
    <automated>cd /home/amandeep_chadda/claude/dental-notes && python -m pytest tests/test_session_store.py tests/test_clinical_models.py -x --tb=short</automated>
  </verify>
  <done>SessionStore persists sessions as JSON files with CRUD + finalize. SoapNote includes medications and va_narrative. All tests pass. Existing 182+ tests unbroken.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clipboard text formatter for SOAP notes</name>
  <files>src/dental_notes/clinical/formatter.py, tests/test_note_formatter.py</files>
  <behavior>
    - Test: format_note_for_clipboard(soap_note) returns plain text with section headers (Subjective, Objective, Assessment, Plan, CDT Codes, Clinical Discussion, Prescribed Medications)
    - Test: sections with empty content are omitted
    - Test: CDT codes formatted as "D1234 - Description" per line
    - Test: Clinical Discussion formatted as bullet points ("- item")
    - Test: Medications formatted as bullet points at bottom of note
    - Test: VA narrative appended as separate section when present (not None)
    - Test: VA narrative omitted when va_narrative is None
    - Test: format_section(section_name, content) returns just that section's text for per-section copy
    - Test: format from edited_note dict works (user edits override extraction_result)
  </behavior>
  <action>
    Create src/dental_notes/clinical/formatter.py with:

    1. format_note_for_clipboard(soap_note: SoapNote | None = None, edited_note: dict | None = None) -> str:
       - If edited_note is provided, use its values (user edits take priority per locked decision)
       - If soap_note is provided without edited_note, use SoapNote fields
       - Build plain text with section headers separated by blank lines:
         "Subjective:\n{text}\n\nObjective:\n{text}\n\nAssessment:\n{text}\n\nPlan:\n{text}\n\nCDT Codes:\n{codes}\n\nClinical Discussion:\n{bullets}\n\nPrescribed Medications:\n{meds}"
       - CDT codes: each on its own line as "D1234 - Description"
       - Clinical Discussion: each item prefixed with "- "
       - Medications: each item prefixed with "- "
       - VA Narrative: if present, appended as "VA Per-Tooth Narrative:\n{text}"
       - Skip sections with empty/blank content
       - Clipboard format per user decision: plain text with section headers

    2. format_section(section_name: str, content: str | list | None) -> str:
       - Format a single section for per-section copy
       - Handles str (narrative sections), list[str] (clinical discussion, meds), list[CdtCode] or str (cdt codes)
       - Returns "{Section Name}:\n{content}"

    Create tests/test_note_formatter.py testing all behaviors above. Use the SoapNote model from clinical/models.py to construct test inputs.
  </action>
  <verify>
    <automated>cd /home/amandeep_chadda/claude/dental-notes && python -m pytest tests/test_note_formatter.py -x --tb=short</automated>
  </verify>
  <done>NoteFormatter produces correctly structured plain text for clipboard copy (Copy All and per-section). All formatting tests pass. Existing tests unbroken.</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd /home/amandeep_chadda/claude/dental-notes && python -m pytest tests/ -x --tb=short` -- all tests pass (182+ existing + new)
2. SessionStore creates/reads/updates/deletes JSON files in sessions/ directory
3. SoapNote model includes medications and va_narrative fields
4. NoteFormatter produces clipboard-ready text with proper section headers
5. Finalize deletes transcript file safely (missing_ok=True)
</verification>

<success_criteria>
- SavedSession + SessionStore fully tested with all CRUD operations
- SoapNote enriched with medications (list[str]) and va_narrative (str | None)
- EXTRACTION_SYSTEM_PROMPT updated with medications and VA detection instructions
- NoteFormatter produces Copy All text matching the locked decision format
- Finalize safely deletes transcript (AUD-02)
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-and-export/03-01-SUMMARY.md`
</output>
