---
phase: 03-review-and-export
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/dental_notes/ui/routes.py
  - src/dental_notes/main.py
  - src/dental_notes/templates/index.html
  - src/dental_notes/templates/review.html
  - src/dental_notes/templates/_review_note.html
  - src/dental_notes/templates/_review_transcript.html
  - src/dental_notes/templates/_session_list.html
  - src/dental_notes/static/style.css
  - src/dental_notes/static/review.js
  - tests/test_review_routes.py
  - tests/conftest.py
autonomous: true
requirements: [REV-01, REV-02, REV-03, AUD-02]

must_haves:
  truths:
    - "User sees transcript on the left and SOAP note on the right in a 50/50 split"
    - "User can edit any section of the SOAP note via textareas"
    - "User can edit the transcript via textarea"
    - "Editing transcript shows a 'Transcript changed -- Regenerate note?' banner"
    - "Copy All copies entire formatted note to clipboard"
    - "Per-section copy icons copy individual sections"
    - "Session list shows saved sessions with timestamp, preview, and status badge"
    - "Finalize & Clear deletes transcript and shows confirmation"
    - "Regenerate button re-runs extraction on current transcript"
    - "Session stop auto-extracts SOAP note and redirects to review"
  artifacts:
    - path: "src/dental_notes/ui/routes.py"
      provides: "Review, session list, finalize, extract, save routes"
      contains: "review"
    - path: "src/dental_notes/templates/review.html"
      provides: "Side-by-side review page"
      min_lines: 50
    - path: "src/dental_notes/static/review.js"
      provides: "Clipboard copy, dirty tracking, auto-resize"
      min_lines: 40
    - path: "src/dental_notes/templates/_session_list.html"
      provides: "Session list partial"
      min_lines: 15
    - path: "tests/test_review_routes.py"
      provides: "Route tests for review workflow"
      min_lines: 100
  key_links:
    - from: "src/dental_notes/ui/routes.py"
      to: "src/dental_notes/session/store.py"
      via: "Routes use SessionStore for persistence"
      pattern: "SessionStore|get_session|list_sessions"
    - from: "src/dental_notes/ui/routes.py"
      to: "src/dental_notes/clinical/extractor.py"
      via: "Extract route calls extract_with_gpu_handoff"
      pattern: "extract_with_gpu_handoff"
    - from: "src/dental_notes/ui/routes.py"
      to: "src/dental_notes/clinical/formatter.py"
      via: "Copy route uses format_note_for_clipboard"
      pattern: "format_note_for_clipboard"
    - from: "src/dental_notes/static/review.js"
      to: "navigator.clipboard"
      via: "Browser clipboard API"
      pattern: "navigator\\.clipboard\\.writeText"
    - from: "src/dental_notes/templates/review.html"
      to: "src/dental_notes/static/review.js"
      via: "Script tag"
      pattern: "review\\.js"
---

<objective>
Build the review UI: side-by-side review screen, session list, editing, clipboard copy, extraction trigger, and finalization flow -- all wired to the data layer from Plan 01.

Purpose: This is the core user-facing plan for Phase 3. The dentist needs to see the transcript alongside the SOAP note, edit freely, copy to clipboard for Dentrix, and finalize (delete transcript). Sessions are listable for batch note-writing.
Output: Working review screen, session list, routes, templates, JavaScript, and test coverage.
</objective>

<execution_context>
@/home/amandeep_chadda/.claude/get-shit-done/workflows/execute-plan.md
@/home/amandeep_chadda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-review-and-export/03-CONTEXT.md
@.planning/phases/03-review-and-export/03-RESEARCH.md
@.planning/phases/03-review-and-export/03-01-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 01 that this plan consumes -->

From src/dental_notes/session/store.py (created in Plan 01):
```python
class SessionStatus(str, Enum):
    RECORDED = "recorded"
    EXTRACTED = "extracted"
    REVIEWED = "reviewed"

class SavedSession(BaseModel):
    session_id: str
    created_at: datetime
    updated_at: datetime
    status: SessionStatus
    transcript_path: str
    chunks: list[tuple[str, str]]
    extraction_result: ExtractionResult | None
    edited_note: dict | None
    transcript_dirty: bool

class SessionStore:
    def __init__(self, sessions_dir: Path) -> None: ...
    def create_session(self, chunks, transcript_path) -> SavedSession: ...
    def get_session(self, session_id) -> SavedSession | None: ...
    def list_sessions(self) -> list[SavedSession]: ...
    def update_session(self, session) -> None: ...
    def delete_session(self, session_id) -> None: ...
    def finalize_session(self, session_id) -> None: ...
```

From src/dental_notes/clinical/formatter.py (created in Plan 01):
```python
def format_note_for_clipboard(soap_note=None, edited_note=None) -> str: ...
def format_section(section_name, content) -> str: ...
```

From src/dental_notes/clinical/models.py (enriched in Plan 01):
```python
class SoapNote(BaseModel):
    subjective: str
    objective: str
    assessment: str
    plan: str
    cdt_codes: list[CdtCode]
    clinical_discussion: list[str]
    medications: list[str]          # NEW
    va_narrative: str | None        # NEW
```

From src/dental_notes/clinical/extractor.py:
```python
class ClinicalExtractor:
    def extract_from_chunks(self, chunks) -> ExtractionResult: ...
    def extract_with_gpu_handoff(self, transcript, whisper_service) -> ExtractionResult: ...
```

From src/dental_notes/ui/routes.py (existing):
```python
router = APIRouter()
def _get_session_manager(request): ...
def _get_templates(request): ...
```

From src/dental_notes/main.py (existing):
```python
templates = Jinja2Templates(directory=str(_TEMPLATES_DIR))
app.state.session_manager = session_manager
app.state.whisper_service = whisper_service
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Review and session list routes with tests</name>
  <files>src/dental_notes/ui/routes.py, src/dental_notes/main.py, tests/test_review_routes.py, tests/conftest.py</files>
  <behavior>
    - Test: POST /session/stop saves session via SessionStore, triggers extraction in thread pool, redirects to /session/{id}/review via HX-Redirect
    - Test: GET /session/{id}/review renders review.html with transcript and SOAP note data
    - Test: GET /session/{id}/review returns 404 if session not found
    - Test: POST /session/{id}/extract runs extraction via thread pool executor, updates session, returns note panel HTML
    - Test: POST /session/{id}/save persists edited note fields, updates session status to REVIEWED
    - Test: POST /session/{id}/finalize deletes transcript file and session, returns confirmation HTML
    - Test: GET /sessions returns session list page
    - Test: GET /api/session/{id}/note-text returns formatted note text for clipboard (server-side formatting)
    - Test: Extraction error returns error HTML (not 500 crash)
  </behavior>
  <action>
    1. Update src/dental_notes/main.py:
       - Import SessionStore from dental_notes.session.store
       - In lifespan: create SessionStore(settings.sessions_dir), assign to app.state.session_store
       - Import ClinicalExtractor and OllamaService
       - In lifespan: create OllamaService(settings.ollama_host, settings.ollama_model), create ClinicalExtractor(ollama_service, settings), assign to app.state.clinical_extractor and app.state.ollama_service

    2. Update src/dental_notes/ui/routes.py with new routes:
       - Add helper: `_get_session_store(request)` -> request.app.state.session_store
       - Add helper: `_get_extractor(request)` -> request.app.state.clinical_extractor

       - Modify POST /session/stop:
         * After session_manager.stop(), create session via session_store.create_session(chunks, transcript_path)
         * Run extraction in thread pool: `await asyncio.get_event_loop().run_in_executor(None, lambda: extractor.extract_with_gpu_handoff(transcript_text, whisper_service))`
         * Update session with extraction_result, set status to EXTRACTED
         * Return HX-Redirect to /session/{session_id}/review
         * If extraction fails, still save session (status RECORDED) and redirect to review with error flag
         * Per user decision: auto-extract on session stop

       - Add GET /session/{session_id}/review:
         * Load session from store
         * Return 404 if not found
         * Render review.html with session data (chunks, extraction_result, edited_note)
         * Pass transcript_text (formatted from chunks), soap_note fields (from edited_note if exists, else extraction_result)

       - Add POST /session/{session_id}/extract:
         * Load session, re-extract using current transcript (may have been edited)
         * Run extraction in thread pool executor (CRITICAL: do not block asyncio event loop -- per pitfall #2 in research)
         * GPU handoff: use extract_with_gpu_handoff() (per locked decision -- never call extract() directly from route)
         * Update session extraction_result, clear edited_note, set transcript_dirty=False
         * Return _review_note.html partial for HTMX swap

       - Add POST /session/{session_id}/save:
         * Accept Form fields: subjective, objective, assessment, plan, cdt_codes, clinical_discussion, medications, transcript
         * Store in session.edited_note dict
         * Set status to REVIEWED
         * Return save confirmation HTML snippet

       - Add POST /session/{session_id}/finalize:
         * Call session_store.finalize_session(session_id)
         * Return confirmation HTML with "New Session" and "Session List" links

       - Add GET /sessions:
         * List all sessions via session_store.list_sessions()
         * Render session list page (can be index.html extended or sessions.html)
         * Per user decision: session list is the home screen between appointments

       - Add GET /api/session/{session_id}/note-text:
         * Load session, format note via format_note_for_clipboard
         * Return plain text response (used by JavaScript for clipboard copy)
         * This server-side formatting avoids duplicating formatting logic in JS

       - Modify GET / (index):
         * Add session_store to context, pass saved sessions list
         * Session list appears below recording controls per research recommendation

    3. Update tests/conftest.py:
       - Ensure FakeSessionStore is available (from Plan 01)
       - Add app fixture that includes session_store in app.state
       - Add fake_extractor fixture returning a ClinicalExtractor with FakeOllamaService

    4. Create tests/test_review_routes.py:
       - Use httpx AsyncClient with the app fixture
       - Test all routes listed in behavior section
       - For extraction routes, use FakeOllamaService (not real Ollama)
       - Test error cases (404, extraction failure)

    IMPORTANT: Use `def` (not `async def`) for routes that call blocking OllamaService/ClinicalExtractor. FastAPI automatically runs sync route handlers in a thread pool, avoiding the pitfall of blocking the event loop. Alternatively, use `async def` with `run_in_executor`. Choose one approach consistently. The simpler approach is sync `def` for extraction-heavy routes.
  </action>
  <verify>
    <automated>cd /home/amandeep_chadda/claude/dental-notes && python -m pytest tests/test_review_routes.py -x --tb=short</automated>
  </verify>
  <done>All review and session routes work correctly. Session stop auto-extracts and redirects to review. Sessions are listable. Edits persist. Finalize deletes transcript. All route tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Review templates, session list UI, and JavaScript</name>
  <files>src/dental_notes/templates/review.html, src/dental_notes/templates/_review_note.html, src/dental_notes/templates/_review_transcript.html, src/dental_notes/templates/_session_list.html, src/dental_notes/templates/index.html, src/dental_notes/static/review.js, src/dental_notes/static/style.css</files>
  <action>
    1. Create src/dental_notes/templates/review.html:
       - Full HTML page extending the project style (same header/footer as index.html)
       - Include HTMX from CDN (same as index.html)
       - Include review.js script
       - CSS Grid 50/50 layout: `.review-container { display: grid; grid-template-columns: 1fr 1fr; gap: 1rem; height: calc(100vh - 120px); }`
       - Left panel (transcript):
         * Header: "Transcript" + copy icon button
         * Single large textarea with id="transcript-edit", filled with transcript text (speaker labels included as "Doctor: text\n\nPatient: text")
         * Auto-resizing textarea
       - Right panel (clinical note):
         * Header: "Clinical Note" + "Copy All" button + "Regenerate" button
         * Include _review_note.html partial (swappable via HTMX)
       - Regenerate button: `hx-post="/session/{{ session_id }}/extract" hx-target="#note-panel" hx-swap="innerHTML" hx-indicator="#extraction-spinner"`
       - Extraction spinner: hidden by default, shown via htmx-indicator class during extraction
       - Regen banner: hidden div `id="regen-banner"` with text "Transcript changed -- Regenerate note?" and a Regenerate button, shown via JavaScript when transcript is dirty
       - Bottom action bar:
         * "Save" button: `hx-post="/session/{{ session_id }}/save" hx-include="closest form"` (wraps all textareas in a form)
         * "Finalize & Clear" button: `hx-post="/session/{{ session_id }}/finalize" hx-confirm="This will permanently delete the transcript. Are you sure?"` (hx-confirm for two-step per locked decision)
         * "Back to Sessions" link
       - Per user decision: no read-only sections, everything in textareas

    2. Create src/dental_notes/templates/_review_note.html:
       - Partial for HTMX swap (used by extract and review page)
       - Textarea for each SOAP section: subjective, objective, assessment, plan
       - Each textarea has name matching the field, prefilled from soap_note or edited_note
       - Per-section copy icon button next to each section label (calls copySection(name) JS function)
       - CDT codes textarea: one code per line formatted as "D1234 - Description"
       - Clinical Discussion textarea: bullet points as lines prefixed with "- "
       - Medications textarea: bullet points as lines prefixed with "- "
       - VA Narrative textarea: only rendered if va_narrative is not None (conditional Jinja2 block)
       - All textareas auto-resize on input
       - Per user decision: CDT codes fully editable, Clinical Discussion fully editable

    3. Create src/dental_notes/templates/_review_transcript.html:
       - Partial containing the transcript textarea
       - Used by review.html for the left panel content

    4. Create src/dental_notes/templates/_session_list.html:
       - Partial showing list of saved sessions
       - Each session card shows: timestamp (formatted as "Mar 10, 2:30 PM"), first 60 chars of transcript preview, status badge (colored: Recorded=gray, Extracted=blue, Reviewed=green)
       - Each card links to /session/{session_id}/review
       - Sorted by created_at descending (most recent first)
       - If no sessions, show "No saved sessions" message
       - Per user decision: timestamp + first line of transcript preview + status badge

    5. Update src/dental_notes/templates/index.html:
       - Keep existing mic selector and recording controls at top
       - Below the transcript section, add session list section: include _session_list.html
       - Pass sessions list from route context
       - After finalization confirmation, show "New Session" button (returns to index) and "Session List" (scrolls to list)
       - Per user decision: session list is the home screen between appointments

    6. Create src/dental_notes/static/review.js:
       - copyAll() function:
         * Fetch formatted text from /api/session/{session_id}/note-text (server-side formatting)
         * Call copyToClipboard(text) helper
         * Show brief "Copied!" feedback near the button
       - copySection(sectionName) function:
         * Read the textarea value for that section
         * Prepend section header (capitalize section name + ":")
         * Call copyToClipboard(text)
       - copyToClipboard(text) helper:
         * Use navigator.clipboard.writeText() if window.isSecureContext
         * Fallback: hidden textarea + document.execCommand('copy') for non-secure contexts
         * Per pitfall #1: always use localhost, but include fallback for robustness
       - Dirty tracking:
         * Store original transcript value on page load
         * On transcript textarea input event, compare to original
         * If changed: show #regen-banner, if matches original: hide banner
         * Per user decision: "Transcript changed -- Regenerate note?"
       - Auto-resize textareas:
         * On DOMContentLoaded and on input events for all textareas
         * Set height = 'auto' then height = scrollHeight + 'px'
         * Per pitfall #4: avoid tiny textareas that cut off content
       - showCopyFeedback(buttonElement, message):
         * Brief tooltip/message near the button indicating success
         * Auto-dismiss after 2 seconds

    7. Update src/dental_notes/static/style.css:
       - Add review-container grid styles (50/50, full height, gap)
       - Add panel styles (border, rounded corners, overflow-y: auto for independent scroll)
       - Add panel-header styles (sticky top, background, padding)
       - Add textarea styles for SOAP sections (full width, border, padding, font-family: inherit, resize: none since auto-resize handles it)
       - Add note-section styles (label + copy icon inline, textarea below)
       - Add regen-banner styles (warning color, padding, hidden by default)
       - Add session-list styles (card layout for each session)
       - Add status-badge styles (colored badges for Recorded/Extracted/Reviewed)
       - Add extraction-spinner styles (.htmx-indicator hidden by default, shown during request)
       - Add finalize-confirmation styles
       - Add btn-copy, btn-icon styles for copy buttons
       - Per user decision: both panels scroll independently
  </action>
  <verify>
    <automated>cd /home/amandeep_chadda/claude/dental-notes && python -m pytest tests/test_review_routes.py tests/test_routes.py -x --tb=short</automated>
  </verify>
  <done>Review page renders with 50/50 split layout. All SOAP sections editable in textareas. Copy All and per-section copy work via JavaScript. Session list shows saved sessions. Transcript dirty tracking shows regeneration banner. All route tests still pass (existing + new).</done>
</task>

</tasks>

<verification>
After both tasks complete:
1. `cd /home/amandeep_chadda/claude/dental-notes && python -m pytest tests/ -x --tb=short` -- all tests pass
2. Review page renders transcript left, SOAP note right in 50/50 split
3. All SOAP sections and transcript are editable textareas
4. Copy All produces formatted text with section headers
5. Session list shows sessions with timestamp, preview, status badge
6. Finalize deletes transcript with confirmation
7. Regenerate re-runs extraction
8. Session stop auto-extracts and redirects to review
</verification>

<success_criteria>
- Review page loads with 50/50 split (REV-01)
- All sections editable via textareas (REV-02)
- Copy All copies formatted note to clipboard (REV-03)
- Per-section copy icons work
- Session list shows saved sessions with status badges
- Finalize & Clear deletes transcript (AUD-02)
- Transcript edit triggers regeneration banner
- Extraction runs in thread pool (not blocking event loop)
- All existing tests still pass
</success_criteria>

<output>
After completion, create `.planning/phases/03-review-and-export/03-02-SUMMARY.md`
</output>
