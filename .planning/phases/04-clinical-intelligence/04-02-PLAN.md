---
phase: 04-clinical-intelligence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dental_notes/clinical/models.py
  - src/dental_notes/clinical/prompts.py
  - src/dental_notes/clinical/extractor.py
  - src/dental_notes/clinical/formatter.py
  - src/dental_notes/session/store.py
  - tests/test_clinical_models.py
  - tests/test_prompts.py
  - tests/test_extractor.py
  - tests/test_session_store.py
  - tests/test_note_formatter.py
  - tests/conftest.py
autonomous: true
requirements:
  - CLI-06
  - REV-04

must_haves:
  truths:
    - "AppointmentType enum has all 5 appointment types plus 'general' default"
    - "Each appointment template has a prompt overlay that composes with the base EXTRACTION_SYSTEM_PROMPT"
    - "Template overlays are SHORT (3-5 lines of emphasis) not full prompt rewrites"
    - "ClinicalExtractor.extract() accepts optional template_type parameter and uses composed prompt"
    - "extract_with_gpu_handoff() generates patient summary as second LLM call between SOAP extraction and Ollama unload"
    - "PatientSummary model captures what-we-did, what-comes-next, and home-care sections"
    - "Patient summary prompt instructs 6th-grade reading level with plain language and forbidden clinical jargon"
    - "Patient summary uses TRANSCRIPT as input (not SOAP note) to avoid jargon bleed"
    - "SavedSession stores appointment_type and patient_summary fields"
    - "ExtractionResult carries patient_summary alongside soap_note"
  artifacts:
    - path: "src/dental_notes/clinical/models.py"
      provides: "AppointmentType enum, PatientSummary model, updated ExtractionResult"
      exports: ["AppointmentType", "PatientSummary"]
    - path: "src/dental_notes/clinical/prompts.py"
      provides: "TEMPLATE_OVERLAYS dict, compose_extraction_prompt(), PATIENT_SUMMARY_PROMPT"
      exports: ["TEMPLATE_OVERLAYS", "compose_extraction_prompt", "PATIENT_SUMMARY_PROMPT"]
    - path: "src/dental_notes/clinical/extractor.py"
      provides: "Template-aware extract() and patient summary generation in GPU handoff"
      contains: "_generate_patient_summary"
    - path: "src/dental_notes/session/store.py"
      provides: "SavedSession with appointment_type and patient_summary fields"
      contains: "appointment_type"
    - path: "tests/test_prompts.py"
      provides: "Tests for template composition and patient summary prompt"
      min_lines: 40
  key_links:
    - from: "src/dental_notes/clinical/extractor.py"
      to: "src/dental_notes/clinical/prompts.py"
      via: "compose_extraction_prompt(template_type)"
      pattern: "compose_extraction_prompt"
    - from: "src/dental_notes/clinical/extractor.py"
      to: "src/dental_notes/clinical/models.py"
      via: "PatientSummary model validation"
      pattern: "PatientSummary"
    - from: "src/dental_notes/clinical/extractor.py"
      to: "PATIENT_SUMMARY_PROMPT"
      via: "second LLM call in GPU handoff"
      pattern: "PATIENT_SUMMARY_PROMPT"
    - from: "src/dental_notes/session/store.py"
      to: "src/dental_notes/clinical/models.py"
      via: "ExtractionResult with patient_summary"
      pattern: "patient_summary"
---

<objective>
Add appointment-type templates with prompt composition (CLI-06) and patient summary generation (REV-04) at the backend/model level.

Purpose: Template-specific extraction produces more focused SOAP notes (e.g., restorative templates emphasize materials/shade/anesthetic, hygiene templates emphasize probing/BOP/home care). Patient summary provides a plain-language handout for patients at ~6th-grade reading level, generated as a second LLM call during the existing GPU handoff window.

Output: AppointmentType enum, PatientSummary model, template prompt overlays, composed extraction pipeline, patient summary generation in GPU handoff, updated session persistence with new fields.
</objective>

<execution_context>
@/home/amandeep_chadda/.claude/get-shit-done/workflows/execute-plan.md
@/home/amandeep_chadda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-clinical-intelligence/04-CONTEXT.md
@.planning/phases/04-clinical-intelligence/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/dental_notes/clinical/models.py:
```python
class CdtCode(BaseModel):
    code: str = Field(pattern=r"^D\d{4}$", description="CDT code, e.g. D2391")
    description: str

class SpeakerChunk(BaseModel):
    chunk_id: int
    speaker: str = Field(description="Doctor or Patient")
    text: str

class SoapNote(BaseModel):
    subjective: str
    objective: str
    assessment: str
    plan: str
    cdt_codes: list[CdtCode]
    clinical_discussion: list[str]
    medications: list[str] = Field(default_factory=list)
    va_narrative: str | None = None

class ExtractionResult(BaseModel):
    soap_note: SoapNote
    speaker_chunks: list[SpeakerChunk]
    clinical_summary: str
```

From src/dental_notes/clinical/extractor.py:
```python
class ClinicalExtractor:
    def __init__(self, ollama_service, settings: Settings) -> None: ...
    def extract(self, transcript: str) -> ExtractionResult: ...
    def extract_from_chunks(self, chunks: list[tuple[str, str]]) -> ExtractionResult: ...
    def extract_with_gpu_handoff(self, transcript: str, whisper_service) -> ExtractionResult: ...
```

From src/dental_notes/clinical/prompts.py:
```python
CDT_REFERENCE = """D0120: Periodic oral evaluation..."""
EXTRACTION_SYSTEM_PROMPT = f"""You are a dental clinical note assistant..."""  # ~193 lines
```

From src/dental_notes/session/store.py:
```python
class SavedSession(BaseModel):
    session_id: str
    created_at: datetime
    updated_at: datetime
    status: SessionStatus
    transcript_path: str
    chunks: list[tuple[str, str]]
    extraction_result: ExtractionResult | None = None
    edited_note: dict | None = None
    transcript_dirty: bool = False
```

From src/dental_notes/clinical/formatter.py (format_note_for_clipboard):
```python
def format_note_for_clipboard(soap_note, edited_note=None) -> str: ...
```

From tests/conftest.py:
```python
class FakeOllamaService:
    def generate_structured(self, system_prompt, user_content, schema, **kwargs) -> str: ...
    # call_count, unload_count tracked
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add AppointmentType enum, PatientSummary model, template overlays, and patient summary prompt (CLI-06 + REV-04 models/prompts)</name>
  <files>
    src/dental_notes/clinical/models.py
    src/dental_notes/clinical/prompts.py
    tests/test_clinical_models.py
    tests/test_prompts.py
  </files>
  <behavior>
    - Test: AppointmentType enum has values: general, comprehensive_exam, restorative, hygiene_recall, endodontic, oral_surgery
    - Test: AppointmentType.GENERAL is the default (str value "general")
    - Test: PatientSummary model has fields: what_we_did (str), whats_next (str), home_care (str)
    - Test: PatientSummary model validates successfully with sample data
    - Test: ExtractionResult has optional patient_summary field (PatientSummary | None, default None)
    - Test: TEMPLATE_OVERLAYS dict has keys matching all AppointmentType values except "general"
    - Test: Each template overlay is short (under 500 chars / ~5 lines)
    - Test: compose_extraction_prompt(None) returns base EXTRACTION_SYSTEM_PROMPT unchanged
    - Test: compose_extraction_prompt("general") returns base EXTRACTION_SYSTEM_PROMPT unchanged
    - Test: compose_extraction_prompt("restorative") returns base prompt + restorative overlay appended
    - Test: compose_extraction_prompt("comprehensive_exam") includes comprehensive exam emphasis
    - Test: PATIENT_SUMMARY_PROMPT contains "6th-grade" or "6th grade" reading level instruction
    - Test: PATIENT_SUMMARY_PROMPT contains forbidden terms list (CDT codes, Latin terms, abbreviations)
    - Test: PATIENT_SUMMARY_PROMPT instructs using transcript (not SOAP note) as input
  </behavior>
  <action>
1. Update `src/dental_notes/clinical/models.py`:
   - Add `AppointmentType(str, Enum)` with 6 values: GENERAL="general", COMPREHENSIVE_EXAM="comprehensive_exam", RESTORATIVE="restorative", HYGIENE_RECALL="hygiene_recall", ENDODONTIC="endodontic", ORAL_SURGERY="oral_surgery".
   - Add `PatientSummary(BaseModel)` with fields: `what_we_did: str`, `whats_next: str`, `home_care: str`. Use descriptive Field descriptions per research.
   - Add `patient_summary: PatientSummary | None = None` field to `ExtractionResult` (default None for backward compatibility).

2. Update `src/dental_notes/clinical/prompts.py`:
   - Add `TEMPLATE_OVERLAYS: dict[str, str]` with 5 entries (one per non-general appointment type). Keep each overlay SHORT (3-5 lines) per pitfall #2 guidance. Overlays adjust emphasis only, not restate SOAP structure:
     - comprehensive_exam: emphasize full-mouth findings, perio assessment, radiographic review, treatment planning, all teeth examined
     - restorative: emphasize anesthetic type/amount/site, material and shade selection, step-by-step procedure narrative, post-op instructions
     - hygiene_recall: emphasize probing depths, BOP, calculus, home care instructions, fluoride, recall interval
     - endodontic: emphasize vitality tests, working length, irrigation, obturation, canal count, post-op instructions
     - oral_surgery: emphasize surgical approach, anesthesia details, bone graft material, suture type/count, hemostasis, post-surgical instructions
   - Add `compose_extraction_prompt(template_type: str | None = None) -> str`: returns base EXTRACTION_SYSTEM_PROMPT if template_type is None or "general", otherwise appends the matching overlay.
   - Add `PATIENT_SUMMARY_PROMPT` constant. Use research code example as starting point. Key rules:
     - Input is the TRANSCRIPT (not SOAP note) to avoid jargon bleed (per pitfall #4)
     - 6th-grade reading level, plain friendly language
     - 3 sections: what we did today, what comes next, home care instructions
     - Forbidden terms: CDT codes, Latin terms, medical abbreviations
     - Positive examples of plain-language replacements
     - Max 250 words
     - Use "you/your" voice, short sentences

3. Write tests FIRST:
   - Create `tests/test_prompts.py`: Test compose_extraction_prompt with None/general/each template type. Test PATIENT_SUMMARY_PROMPT content requirements.
   - Update `tests/test_clinical_models.py`: Test AppointmentType enum values, PatientSummary model validation, ExtractionResult backward compatibility (patient_summary defaults to None).
  </action>
  <verify>
    <automated>cd /home/amandeep_chadda/claude/dental-notes && python -m pytest tests/test_clinical_models.py tests/test_prompts.py -x -q --tb=short</automated>
  </verify>
  <done>
    - AppointmentType enum has all 6 values
    - PatientSummary model validates with 3 fields
    - ExtractionResult.patient_summary is optional and defaults to None (backward compatible)
    - TEMPLATE_OVERLAYS has 5 entries, all short
    - compose_extraction_prompt() works for all template types
    - PATIENT_SUMMARY_PROMPT meets all content requirements (reading level, forbidden terms, transcript input)
    - All model and prompt tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Template-aware extraction, patient summary generation in GPU handoff, session persistence updates (CLI-06 + REV-04 pipeline)</name>
  <files>
    src/dental_notes/clinical/extractor.py
    src/dental_notes/clinical/formatter.py
    src/dental_notes/session/store.py
    tests/test_extractor.py
    tests/test_session_store.py
    tests/test_note_formatter.py
    tests/conftest.py
  </files>
  <behavior>
    - Test: ClinicalExtractor.extract() accepts optional template_type parameter
    - Test: extract(transcript, template_type="restorative") passes composed prompt to OllamaService
    - Test: extract(transcript) without template_type uses base prompt (backward compatible)
    - Test: extract_with_gpu_handoff() accepts optional template_type parameter
    - Test: extract_with_gpu_handoff() calls OllamaService twice (once for SOAP, once for patient summary)
    - Test: extract_with_gpu_handoff() returns ExtractionResult with patient_summary populated
    - Test: _generate_patient_summary() uses PATIENT_SUMMARY_PROMPT as system prompt
    - Test: _generate_patient_summary() passes transcript text (not SOAP note) as user content
    - Test: _generate_patient_summary() uses lower num_ctx (4096) for faster generation
    - Test: GPU handoff still ensures Whisper reload in finally block even if summary fails
    - Test: SavedSession has appointment_type field (AppointmentType, default GENERAL)
    - Test: SavedSession has patient_summary field (PatientSummary | None, default None)
    - Test: SavedSession serializes and deserializes with new fields (backward compatible)
    - Test: format_patient_summary_for_clipboard returns plain text with section headers
    - Test: FakeOllamaService supports multiple call responses for 2-call GPU handoff
  </behavior>
  <action>
1. Update `tests/conftest.py`:
   - Modify `FakeOllamaService` to support multiple response_data values: accept `response_data` as either a dict (single response for all calls, backward compatible) or a list of dicts (one per call). On each `generate_structured()` call, return `response_data[call_count - 1]` if list, else `response_data`.
   - Add a canned PatientSummary response dict for the second call in GPU handoff tests.

2. Update `src/dental_notes/clinical/extractor.py`:
   - Add `template_type: str | None = None` parameter to `extract()`. Import and call `compose_extraction_prompt(template_type)` to get the system prompt instead of using `EXTRACTION_SYSTEM_PROMPT` directly.
   - Add `template_type: str | None = None` parameter to `extract_from_chunks()`, forwarding to `extract()`.
   - Add `template_type: str | None = None` parameter to `extract_with_gpu_handoff()`.
   - Add private method `_generate_patient_summary(self, transcript: str) -> PatientSummary`:
     - Import `PATIENT_SUMMARY_PROMPT` from prompts.py and `PatientSummary` from models.py.
     - Call `self._ollama.generate_structured()` with PATIENT_SUMMARY_PROMPT, the TRANSCRIPT text as user content (not the SOAP note -- per pitfall #4), PatientSummary schema, and `num_ctx=4096` for faster generation.
     - Validate response with `PatientSummary.model_validate_json()`.
   - Update `extract_with_gpu_handoff()`: after `self.extract()`, call `self._generate_patient_summary(transcript)` and assign to `result.patient_summary`. Wrap summary generation in a try/except so a summary failure does NOT block the extraction result (log warning, leave patient_summary as None). Keep the `finally` block unchanged (ollama unload + whisper reload).

3. Update `src/dental_notes/session/store.py`:
   - Add `appointment_type: str = "general"` field to `SavedSession` (using str for JSON serialization simplicity, but store AppointmentType enum values).
   - Add `patient_summary: dict | None = None` field to `SavedSession` (store as dict for JSON compat, convert from PatientSummary.model_dump() on save).

4. Update `src/dental_notes/clinical/formatter.py`:
   - Add `format_patient_summary_for_clipboard(patient_summary) -> str` function. Formats PatientSummary (or dict with same keys) as plain text with section headers: "WHAT WE DID TODAY", "WHAT COMES NEXT", "HOME CARE INSTRUCTIONS".

5. Write tests FIRST:
   - Update `tests/test_extractor.py`: Test template_type parameter on extract(), verify composed prompt passed to ollama. Test extract_with_gpu_handoff() makes 2 ollama calls. Test patient_summary populated in result. Test summary failure does not break extraction.
   - Update `tests/test_session_store.py`: Test SavedSession with appointment_type and patient_summary fields. Test JSON round-trip with new fields. Test backward compat (old sessions without new fields load correctly).
   - Update `tests/test_note_formatter.py`: Test format_patient_summary_for_clipboard output format.
  </action>
  <verify>
    <automated>cd /home/amandeep_chadda/claude/dental-notes && python -m pytest tests/test_extractor.py tests/test_session_store.py tests/test_note_formatter.py tests/conftest.py -x -q --tb=short</automated>
  </verify>
  <done>
    - ClinicalExtractor.extract() composes prompt from template_type
    - extract_with_gpu_handoff() generates patient summary as second LLM call
    - Patient summary uses transcript as input (not SOAP note)
    - Summary failure is graceful (logs warning, leaves patient_summary None)
    - GPU handoff still ensures Whisper reload in finally block
    - SavedSession persists appointment_type and patient_summary
    - FakeOllamaService supports multi-call responses
    - format_patient_summary_for_clipboard produces clean plain text
    - All extractor, session store, and formatter tests pass
  </done>
</task>

</tasks>

<verification>
Run full test suite to confirm no regressions:
```bash
cd /home/amandeep_chadda/claude/dental-notes && python -m pytest tests/ -q --tb=short
```
All 249+ tests must pass (existing + new).
</verification>

<success_criteria>
- AppointmentType enum, PatientSummary model, and template overlays defined
- compose_extraction_prompt() produces correct composed prompts for all template types
- PATIENT_SUMMARY_PROMPT meets plain-language requirements (6th-grade, forbidden terms, transcript input)
- ClinicalExtractor pipeline is template-aware end-to-end
- Patient summary generated in GPU handoff window with graceful failure handling
- SavedSession persists new fields with backward compatibility
- Patient summary clipboard formatter works
- All tests pass including new prompts, extractor, store, and formatter tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-clinical-intelligence/04-02-SUMMARY.md`
</output>
