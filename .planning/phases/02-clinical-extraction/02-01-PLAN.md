---
phase: 02-clinical-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/dental_notes/clinical/__init__.py
  - src/dental_notes/clinical/models.py
  - src/dental_notes/clinical/prompts.py
  - src/dental_notes/clinical/ollama_service.py
  - src/dental_notes/config.py
  - tests/test_clinical_models.py
  - tests/test_ollama_service.py
  - tests/conftest.py
  - pyproject.toml
autonomous: true
requirements: [CLI-01, CLI-02, CLI-03, CLI-04]

must_haves:
  truths:
    - "Pydantic models validate SOAP note structure with four sections and CDT codes"
    - "OllamaService can check if Ollama is running and model is available"
    - "OllamaService can generate structured output conforming to a JSON schema"
    - "OllamaService can unload a model to free GPU memory"
    - "Config includes Ollama host, model, fallback model, temperature, and context size"
    - "System prompt contains dental SOAP instructions, speaker attribution rules, and CDT reference list"
  artifacts:
    - path: "src/dental_notes/clinical/models.py"
      provides: "SoapNote, CdtCode, SpeakerChunk, ExtractionResult Pydantic models"
      exports: ["SoapNote", "CdtCode", "SpeakerChunk", "ExtractionResult"]
    - path: "src/dental_notes/clinical/prompts.py"
      provides: "System prompt, CDT reference, speaker attribution instructions"
      exports: ["EXTRACTION_SYSTEM_PROMPT", "CDT_REFERENCE"]
    - path: "src/dental_notes/clinical/ollama_service.py"
      provides: "OllamaService with health check, model check, generate, unload"
      exports: ["OllamaService"]
    - path: "src/dental_notes/config.py"
      provides: "Extended Settings with Ollama configuration"
      contains: "ollama_host"
    - path: "tests/test_clinical_models.py"
      provides: "Pydantic model validation tests"
      min_lines: 40
    - path: "tests/test_ollama_service.py"
      provides: "OllamaService unit tests with FakeOllamaService"
      min_lines: 60
  key_links:
    - from: "src/dental_notes/clinical/ollama_service.py"
      to: "ollama.Client"
      via: "import ollama"
      pattern: "from ollama import Client"
    - from: "src/dental_notes/clinical/models.py"
      to: "pydantic.BaseModel"
      via: "import pydantic"
      pattern: "class SoapNote\\(BaseModel\\)"
    - from: "src/dental_notes/clinical/ollama_service.py"
      to: "src/dental_notes/config.py"
      via: "Settings fields"
      pattern: "ollama_host|ollama_model"
---

<objective>
Set up the clinical extraction foundation: Pydantic data models, Ollama service wrapper, system prompts with CDT reference, and extended configuration. This plan creates all the contracts and infrastructure that the extractor and reattributor (Plan 02) will build on.

Purpose: Establish type-safe interfaces and the LLM service layer so Plan 02 can implement clinical logic against defined contracts without ambiguity.
Output: `src/dental_notes/clinical/` module with models, prompts, and OllamaService; extended config; test coverage for models and service.
</objective>

<execution_context>
@/home/amandeep_chadda/.claude/get-shit-done/workflows/execute-plan.md
@/home/amandeep_chadda/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-clinical-extraction/02-CONTEXT.md
@.planning/phases/02-clinical-extraction/02-RESEARCH.md

<interfaces>
<!-- Existing interfaces the executor needs -->

From src/dental_notes/config.py:
```python
from pydantic_settings import BaseSettings, SettingsConfigDict

class Settings(BaseSettings):
    model_config = SettingsConfigDict(env_prefix="DENTAL_")
    whisper_model: str = "small"
    compute_type: str = "int8"
    sample_rate: int = 16000
    vad_threshold: float = 0.5
    max_chunk_duration_secs: int = 20
    silence_gap_secs: float = 1.5
    overlap_secs: float = 1.0
    storage_dir: Path = Path("transcripts")
    host: str = "0.0.0.0"
    port: int = 8000
```

From src/dental_notes/session/manager.py:
```python
class SessionManager:
    def get_chunks(self, start: int = 0) -> list[tuple[str, str]]: ...
    def get_transcript(self) -> str: ...
    # _chunks: list[tuple[str, str]]  # (speaker, text)
```

From src/dental_notes/transcription/whisper_service.py:
```python
class WhisperService:
    def load_model(self) -> None: ...
    def unload(self) -> None: ...
    @property
    def is_loaded(self) -> bool: ...
```

From tests/conftest.py:
```python
class FakeWhisperService:
    def unload(self) -> None: pass
    def load_model(self) -> None: pass
    @property
    def is_loaded(self) -> bool: return True
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pydantic models, config extension, and prompts module</name>
  <files>
    src/dental_notes/clinical/__init__.py,
    src/dental_notes/clinical/models.py,
    src/dental_notes/clinical/prompts.py,
    src/dental_notes/config.py,
    tests/test_clinical_models.py,
    pyproject.toml
  </files>
  <behavior>
    - SoapNote validates with four string fields: subjective, objective, assessment, plan
    - SoapNote has cdt_codes field accepting list of CdtCode objects
    - CdtCode validates code field matches pattern D followed by 4 digits (e.g., "D2391")
    - CdtCode rejects invalid codes (e.g., "X1234", "D12", "D12345")
    - SpeakerChunk has chunk_id (int), speaker (str), text (str)
    - ExtractionResult has soap_note (SoapNote), speaker_chunks (list[SpeakerChunk]), clinical_summary (str)
    - SoapNote.model_json_schema() produces a valid JSON schema usable by Ollama format parameter
    - ExtractionResult.model_json_schema() produces a valid JSON schema
    - EXTRACTION_SYSTEM_PROMPT contains "SOAP", "CDT", "Doctor", "Patient", "D0120" (CDT reference embedded)
    - CDT_REFERENCE contains at least 30 common dental procedure codes
    - Settings.ollama_host defaults to "http://localhost:11434"
    - Settings.ollama_model defaults to "qwen3:8b"
    - Settings.ollama_fallback_model defaults to "qwen3:4b"
    - Settings.ollama_temperature defaults to 0.0
    - Settings.ollama_num_ctx defaults to 8192
    - Settings fields are overridable via DENTAL_ env prefix (e.g., DENTAL_OLLAMA_MODEL)
    - pip install ollama>=0.6.1 added to pyproject.toml dependencies
  </behavior>
  <action>
    **TDD: Write tests first (RED), then implement (GREEN).**

    1. Create `tests/test_clinical_models.py` with tests for:
       - SoapNote construction with valid data
       - SoapNote model_json_schema() produces dict with "properties" key
       - CdtCode validates "D2391" passes, "X1234" rejected, "D12" rejected
       - SpeakerChunk construction
       - ExtractionResult construction with nested SoapNote
       - Config: Settings() has ollama_host, ollama_model, ollama_fallback_model, ollama_temperature, ollama_num_ctx with correct defaults

    2. Run tests -- they MUST fail (models and config fields don't exist yet).

    3. Create `src/dental_notes/clinical/__init__.py` (empty).

    4. Create `src/dental_notes/clinical/models.py` with Pydantic models:
       - CdtCode(BaseModel): code (str, pattern=r"^D\d{4}$"), description (str)
       - SpeakerChunk(BaseModel): chunk_id (int), speaker (str), text (str)
       - SoapNote(BaseModel): subjective (str), objective (str), assessment (str), plan (str), cdt_codes (list[CdtCode])
       - ExtractionResult(BaseModel): soap_note (SoapNote), speaker_chunks (list[SpeakerChunk]), clinical_summary (str)

    5. Create `src/dental_notes/clinical/prompts.py` with:
       - CDT_REFERENCE: string constant with ~35 common CDT codes (D0120 through D9230, from RESEARCH.md)
       - EXTRACTION_SYSTEM_PROMPT: system prompt instructing LLM to filter chitchat, produce SOAP note, suggest CDT codes, re-attribute speakers. Embed CDT_REFERENCE inline. Include speaker attribution rules (Doctor leads/instructs, Patient responds/reports symptoms, continuity across pauses). Include /nothink instruction. See RESEARCH.md for the full prompt template.

    6. Extend `src/dental_notes/config.py` Settings class with:
       - ollama_host: str = "http://localhost:11434"
       - ollama_model: str = "qwen3:8b"
       - ollama_fallback_model: str = "qwen3:4b"
       - ollama_temperature: float = 0.0
       - ollama_num_ctx: int = 8192

    7. Add `"ollama>=0.6.1"` to `pyproject.toml` dependencies list.

    8. Run tests again -- they MUST pass (GREEN).
  </action>
  <verify>
    <automated>cd ~/claude/dental-notes && python3 -m pytest tests/test_clinical_models.py -x --tb=short -q</automated>
  </verify>
  <done>
    - SoapNote, CdtCode, SpeakerChunk, ExtractionResult models exist and validate correctly
    - CdtCode rejects malformed codes via regex pattern
    - model_json_schema() produces valid schemas for Ollama structured output
    - EXTRACTION_SYSTEM_PROMPT and CDT_REFERENCE constants exist with dental clinical content
    - Settings has five Ollama fields with correct defaults
    - ollama>=0.6.1 in pyproject.toml
    - All tests in test_clinical_models.py pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: OllamaService with health check, structured generation, and model unloading</name>
  <files>
    src/dental_notes/clinical/ollama_service.py,
    tests/test_ollama_service.py,
    tests/conftest.py
  </files>
  <behavior>
    - OllamaService(host, model) creates a client connected to the given Ollama host
    - is_available() returns True when Ollama is reachable, False on connection error
    - is_model_ready() returns True when the configured model is pulled, False on 404
    - generate_structured(system_prompt, user_content, schema, temperature, num_ctx) returns raw JSON string
    - generate_structured prepends "/nothink\n\n" to user_content to disable Qwen3 thinking mode
    - unload() sends keep_alive=0 to free GPU memory; does not raise on failure
    - OllamaService logs info on unload success and warning on unload failure
    - FakeOllamaService (in conftest.py) records call_count, last_system_prompt, last_user_content
    - FakeOllamaService.generate_structured returns valid JSON matching ExtractionResult schema
  </behavior>
  <action>
    **TDD: Write tests first (RED), then implement (GREEN).**

    1. Create `tests/test_ollama_service.py` with tests using a FakeOllamaService:
       - test_is_available_returns_true: FakeOllamaService.is_available() returns True
       - test_is_available_returns_false_on_error: A variant that simulates connection error returns False
       - test_is_model_ready_returns_true: when model exists
       - test_is_model_ready_returns_false: when model not pulled (simulate 404)
       - test_generate_structured_returns_json: calls generate_structured, validates returned JSON parses into ExtractionResult
       - test_generate_structured_records_call: verifies last_system_prompt and last_user_content are recorded
       - test_generate_structured_increments_call_count: verifies call_count increments
       - test_unload_does_not_raise: unload() completes without error

       For the real OllamaService (not the fake), test it with mock.patch on the ollama.Client to avoid needing a running Ollama:
       - test_real_service_is_available_true: patch Client.list() to return models
       - test_real_service_is_available_false: patch Client.list() to raise ConnectionError
       - test_real_service_generate_structured: patch Client.chat() to return a mock response with .message.content containing valid JSON
       - test_real_service_unload_success: patch Client.chat() to succeed
       - test_real_service_unload_failure: patch Client.chat() to raise, verify no exception propagates

    2. Run tests -- they MUST fail (OllamaService doesn't exist yet).

    3. Add FakeOllamaService to `tests/conftest.py`:
       - Matches the interface from RESEARCH.md
       - __init__(response_data: dict | None = None) with default ExtractionResult-shaped data
       - is_available() -> True
       - is_model_ready() -> True
       - generate_structured(system_prompt, user_content, schema, **kwargs) -> JSON string
       - Records last_system_prompt, last_user_content, call_count
       - unload() -> None
       - Add a `fake_ollama_service` pytest fixture

    4. Create `src/dental_notes/clinical/ollama_service.py`:
       - OllamaService class per RESEARCH.md Pattern 3
       - __init__(host: str, model: str)
       - is_available() -> bool: try self._client.list(), return True/False
       - is_model_ready() -> bool: try self._client.show(self._model), return True/False, catch ResponseError
       - generate_structured(system_prompt, user_content, schema, temperature=0.0, num_ctx=8192) -> str:
         Calls self._client.chat() with model, messages (system + user with /nothink prefix), format=schema, options dict
         Returns response.message.content
       - unload() -> None: try self._client.chat(model, messages=[], keep_alive=0), log success/warning

    5. Run tests again -- they MUST pass (GREEN).

    6. Run full test suite to verify no regressions: `python3 -m pytest tests/ -x --tb=short`
  </action>
  <verify>
    <automated>cd ~/claude/dental-notes && python3 -m pytest tests/test_ollama_service.py tests/test_clinical_models.py -x --tb=short -q</automated>
  </verify>
  <done>
    - OllamaService exists with is_available, is_model_ready, generate_structured, unload methods
    - FakeOllamaService in conftest.py records calls and returns valid JSON
    - All OllamaService tests pass with mocked Client (no real Ollama needed)
    - Full test suite passes with no regressions
  </done>
</task>

</tasks>

<verification>
- All tests in test_clinical_models.py and test_ollama_service.py pass
- Full test suite passes: `cd ~/claude/dental-notes && python3 -m pytest tests/ -x --tb=short`
- SoapNote.model_json_schema() produces a valid schema
- OllamaService methods are unit tested without requiring a running Ollama instance
- Config Settings includes all Ollama fields with correct defaults
- pyproject.toml includes ollama>=0.6.1 dependency
</verification>

<success_criteria>
- clinical/ module exists with models.py, prompts.py, ollama_service.py
- All Pydantic models validate correctly and produce JSON schemas
- OllamaService wraps ollama.Client with health checks and structured generation
- FakeOllamaService available in conftest.py for downstream plan tests
- Config extended with Ollama settings
- No test regressions from Phase 1/1.1
</success_criteria>

<output>
After completion, create `.planning/phases/02-clinical-extraction/02-01-SUMMARY.md`
</output>
