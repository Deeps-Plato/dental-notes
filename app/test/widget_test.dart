import 'package:flutter_test/flutter_test.dart';

void main() {
  test('placeholder — app boots', () {
    // Real widget tests require ProviderScope + mock DB.
    // See test/domain/ for unit tests.
    expect(1 + 1, 2);
  });
}
